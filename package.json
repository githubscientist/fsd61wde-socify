{
    "name": "fsd61wde-socify",
    "scripts": {
        "build": "npm install --prefix client && npm run build --prefix client",
        "start": "npm install --prefix server && npm start --prefix server"
    },
    "dependencies": {
        "concurrently": "^9.0.1",
        "node": ">=16.13.0"
    }
}
